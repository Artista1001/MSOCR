import os
import fitz

# set the input and output folders

class imgconverter:

 def pdftoimage(file):
    
    
        doc = fitz.open(file)
        
        # loop over all pages and save each page as a JPEG image
        for page in doc:
            # get the page dimensions (in pixels)
            pix = page.get_pixmap(matrix=fitz.Matrix(4, 4), dpi=300, alpha=False, colorspace="sRGB")
            
            # save the image as a JPEG file in the output folder
            
        
        # close the PDF file
        return pix
        doc.close()